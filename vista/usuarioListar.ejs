<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Usuarios Adri</title>
</head>
<body>
    <h1>Lista de Usuarios</h1>
    <a href="/usuario/crear">Crear Nuevo Elemento (Clic) Prueba Adri 1</a>
    <ul>
        <table>
        <% listaUsuario.forEach(function(usuario) { %>            
                    <tr>
                        <td>
                            <strong><%= usuario.id %></strong>
                        </td>
                        <td>
                            <strong><%= usuario.nombre %></strong>
                        </td>
                        <td>
                            (<%= usuario.descripcion %>)
                        </td>
                        <td>
                            <a href="/usuario/modificar/<%= usuario.id %>">Editar</a>
                        </td>
                        <td>
                            <form action="/usuario/eliminar/<%=usuario.id%>" method="POST" style="display: inline;">
                                <input type="hidden" name="_method" value="DELETE">
                                <button type="submit">Eliminar</button>
                            </form>
                        </td>
                    </tr>
                                                                                                        
        <% }); %>
    </table>
    </ul>
</body>
</html>
