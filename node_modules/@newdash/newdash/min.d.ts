/**
 * Computes the minimum value of `array`. If `array` is empty or falsey,
 * `undefined` is returned.
 *
 * @since 5.8.0
 * @category Math
 * @param array The array to iterate over.
 * @returns Returns the minimum value.
 * @example
 *
 * ```js
 * min([4, 2, 8, 6]);
 * // => 2
 *
 * min([]);
 * // => undefined
 * ```
 */
export declare function min<T>(array: ArrayLike<T>): T;
export declare function min(array: any): any;
export default min;
